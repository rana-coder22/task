<form id="edit_form" action="<?= $block->getUrl('grid/index/save') ?>" method="post">
    <input type="hidden" name="form_key" value="<?= $block->getFormKey(); ?>" />
    <input type="hidden" name="id" value="<?= $block->getPost() ? $block->getPost()->getId() : ''; ?>" /> <!-- Add hidden field for ID -->

    <fieldset>
        <div class="admin__field">
            <label class="admin__field-label" for="title"><?= __('Title') ?></label>
            <div class="admin__field-control">
                <input type="text" name="title" id="title" value="<?= $block->getPost() ? $block->getPost()->getTitle() : ''; ?>" required>
            </div>
        </div>
        <div class="admin__field">
            <label class="admin__field-label" for="description"><?= __('Description') ?></label>
            <div class="admin__field-control">
                <textarea name="description" id="description" required><?= $block->getPost() ? $block->getPost()->getDescription() : ''; ?></textarea>
            </div>
        </div>
        <div class="actions">
            <button type="submit" class="action-save primary">
                <?= $block->getPost() ? __('Update') : __('Save') ?> <!-- Change button text based on mode -->
            </button>
        </div>
    </fieldset>
</form>
